/**
 * Display names for legacy persisted financial fields.
 * The underlying `mcm*` keys remain unchanged to preserve existing records
 * and API compatibility.
 */
export const paidPartnerIdLabel = "Paid Partner Id";
export const paidPartnerDateLabel = "Paid Partner Date";
